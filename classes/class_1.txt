Class: Soup

Attributes:
flavor (string)
totalCapacity (integer)
currentCapacity (integer)
isHot (boolean)


Methods:

addHeat (change isHot attribute to true)
addSpice (change the flavor attribute)
eatSoup ( currentCapacity / totalCapacity)
spillSoup (zero out currentCapacity)
